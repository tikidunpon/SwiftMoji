# references:
# * http://www.objc.io/issue-6/travis-ci.html
# * https://github.com/supermarin/xcpretty#usage

language: objective-c
osx_image: xcode7.3
cache: cocoapods
podfile: Example/Podfile

before_install:
  - gem install cocoapods # Since Travis is not always on latest version
  - pod install --project-directory=Example

script:
  - pod lib lint
  - set -o pipefail
  - xcodebuild clean build -workspace Example/SwiftMoji.xcworkspace -scheme SwiftMoji-Example | xcpretty
  - xcodebuild test -workspace Example/SwiftMoji.xcworkspace -scheme SwiftMoji-Example -sdk iphonesimulator -destination "platform=iOS Simulator,name=iPhone 6s" | xcpretty
